{
    "name": "nutrigest",
    "version": "1.0.0",
    "description": "",
    "main": "dist/index.js",
    "scripts": {
        "dev": "nodemon --config nodemon.json --exec npm run dev:start",
        "dev:start": "npx prettier --write . && tslint --project . && rimraf dist && tsc && node dist/index.js",
        "start": "tsc && node dist/index.js",
        "test": "echo \"Error: no test specified\" && exit 1"
    },
    "author": "",
    "license": "ISC",
    "devDependencies": {
        "@types/express": "^4.17.12",
        "nodemon": "^2.0.7",
        "prettier": "^2.3.1",
        "rimraf": "^3.0.2",
        "tslint": "^6.1.3",
        "tslint-config-prettier": "^1.18.0",
        "typescript": "^4.3.4"
    },
    "dependencies": {
        "express": "^4.17.1"
    }
}
